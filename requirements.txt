# Core
numpy>=1.24
pandas>=2.0
Pillow>=10.0
opencv-python>=4.8
matplotlib>=3.8
tqdm>=4.66
PyYAML>=6.0
scikit-learn>=1.3

# Deep learning
# Instala la versión de torch adecuada para tu CUDA desde https://pytorch.org/get-started/locally/
torch>=2.2
torchvision>=0.17
transformers>=4.44
accelerate>=0.30

# COCO annotations
pycocotools>=2.0.7

# Optional: generación automática de máscaras con SAM original de Meta.
# Si falla en Windows, instálalo manualmente desde el repositorio oficial.
git+https://github.com/facebookresearch/segment-anything.git
